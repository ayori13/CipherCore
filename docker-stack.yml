version: "3.9"

services:
  service-logger:
    image: ciphercore-service-logger:latest
    deploy:
      replicas: 2
    ports:
      - "5001:5000"

  service-strength:
    image: ciphercore-service-strength:latest
    deploy:
      replicas: 2
    ports:
      - "5002:5000"
